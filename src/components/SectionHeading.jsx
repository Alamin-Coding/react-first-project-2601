const SectionHeading = ({heading}) => {
  return (
    <div>
      <h2 className="text-[36px] leading-[1.4] text-primary font-bold">
        {heading}
      </h2>
    </div>
  );
};

export default SectionHeading;
